# .env

# =============================================================================
# GitHub Configuration
# =============================================================================
# Generate a personal access token at: https://github.com/settings/tokens
# Required scopes: repo, read:org
GITHUB_TOKEN=
GITHUB_USERNAME=

# =============================================================================
# Backlog Configuration (Multi-Profile Support)
# =============================================================================
# This tool supports multiple Backlog accounts with both .backlog.com and .backlog.jp domains.
# Use the pattern: BACKLOG_<PROFILE>_<SETTING>
#
# Required for each profile:
#   - API_KEY: Generate from your Backlog space settings
#   - HOST: Your Backlog host (e.g., "mycompany.backlog.com" or "projectspace.backlog.jp")
#
# Optional for analysis (use `make list-backlog` to find these):
#   - USER_ID: Your user ID (integer)
#   - PROJECT_ID: Project ID to analyze (integer)
#
# Example: Profile 1 (backlog.com)
BACKLOG_HOGE_API_KEY=
BACKLOG_HOGE_HOST=mycompany.backlog.com
BACKLOG_HOGE_USER_ID=
BACKLOG_HOGE_PROJECT_ID=

# Example: Profile 2 (backlog.jp)
BACKLOG_FUGA_API_KEY=
BACKLOG_FUGA_HOST=projectspace.backlog.jp
BACKLOG_FUGA_USER_ID=
BACKLOG_FUGA_PROJECT_ID=

# To find USER_ID and PROJECT_ID:
#   1. Configure API_KEY and HOST first
#   2. Run: make list-backlog-profiles
#   3. Run: make list-backlog

# =============================================================================
# Calendar Configuration
# =============================================================================
# No environment variables required for calendar analysis.
#
# Manual Setup:
#   1. Export your calendar from Google Calendar:
#      - Open Google Calendar settings (⚙️ → Settings)
#      - Go to "Import & Export"
#      - Click "Export" to download all calendars as a ZIP file
#      - Extract the ZIP file
#   2. Copy your .ics file to: storage/calendar/
#      Example: storage/calendar/your-email@gmail.com.ics
#   3. The analyzer will automatically read all .ics files in storage/calendar/
#
# Note: ICS files include all events (public, private, free/busy)

# =============================================================================
# Notion Configuration
# =============================================================================
# Create an integration at: https://www.notion.so/my-integrations
# Grant the integration access to the pages you want to analyze
NOTION_TOKEN=
# Optional: Specific user ID to filter pages by (if not provided, auto-detected)
NOTION_USER_ID=

# =============================================================================
# Date Range Configuration
# =============================================================================
# Specify the analysis date range in YYYY-MM-DD format
# These dates are used by all analyzers (GitHub, Backlog, Calendar, Notion)
START_DATE=2024-01-01
END_DATE=2024-06-30
